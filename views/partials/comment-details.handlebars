<div class="card" id="blogComments" hidden>
    <div class="card-body">
        <p class="card-text">{{blogs.comments.content}}</p>
        <br>
        <p lass="card-text">- {{name}} on {{datePosted}}</p>
    </div>
    <hr>
</div>
